apiVersion: 1

providers:
  - name: 'Quant dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards # ← 与 docker-compose 的挂载一致
      # 可以放多个 json，都会被导入
